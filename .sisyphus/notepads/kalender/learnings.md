Implemented enhanced /kalender command to display the calendar as an embed using CalendarDisplayService.
- Added optional uke parameter for week navigation (week offset).
- Updated CalendarService to render the embed when the command is invoked with tokens present.
- Maintains Norwegian UI (text and embed content are localized in nb).
- Task completed: /kalender now renders a calendar embed via CalendarDisplayService and supports uke navigation.

# Shared environment variables for Bottus + Nanobot integration
# Source this file in both projects or include in respective .env files

# Bottus (Ine-Discord) - Health API
# Port 3001, requires X-API-Key header
BOTTUS_HEALTH_URL=http://localhost:3001
BOTTUS_HEALTH_API_KEY=your_health_api_key_here
HEALTH_PORT=3001

# Rate limiting (optional)
# RATE_LIMIT=60 requests per window
# RATE_WINDOW_SEC=60 seconds
RATE_LIMIT=60
RATE_WINDOW_SEC=60

# Nanobot - Cron & Reminders
# Port (default 8080)
NANOBOT_PORT=8080
NANOBOT_CRON_DB=/home/reed/.nanobot/cron.db

# Permissions API
# Nanobot queries Bottus for role checks
# GET http://localhost:3001/api/permissions/{userId}/{channelId}
# Returns: { "role": "owner", "permissions": [...] }
